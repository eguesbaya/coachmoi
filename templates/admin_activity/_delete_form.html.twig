<form method="post" action="{{ path('admin_supprimer_activite', {'id': activity.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ activity.id) }}">
    <button  class="btn bg-success text-light">Delete</button>
</form>
