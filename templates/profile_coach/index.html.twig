{% extends 'layout.html.twig' %}

{% block title %}Hello {{ app.user.firstname }}!{% endblock %}

{% block body %}
<div class="example-wrapper">
    <h1>Mon espace Coach</h1>
    <section class="mes-informations container-fluid col-11 border border-dark">
        <div class="profile-h2 border border-dark d-flex align-items-center bg-light w-75 p-0">
            <h2>Mes informations</h2>
            <i class="bi bi-pencil-square"></i>
        </div>
        <div>
            <div>
                <h3>Nom :</h3>
                <p class="client">{{ app.user.lastname }}</p>
            </div>
            <div>
                <h3>Prénom :</h3>
                <p class="client">{{ app.user.firstname }}</p>
            </div>
            <div>
                <h3>Email :</h3>
                <p class="client">{{ app.user.email }}</p>
            </div>
        </div>
    </section>
</div>
{% endblock %}