{{ form_start(formCoach) }}
    {{ form_row(formCoach) }}
    <button class="btn btn-success text-light m-2"><i class="bi bi-check-square-fill text-white"></i> &nbsp; {{ button_label|default('Save') }}</button>
{{ form_end(formCoach) }}