{% extends 'layout.html.twig' %}

{% block title %}Les Coachs
{% endblock %}

{% block body %}
	<section class="container-fluid col-11">
		<h2>
			Tous les coachs
		</h2>

		<div class="m-2 d-flex justify-content-center">
			{{ form_start(form) }}
			{{ form_widget(form) }}
				<button class="btn btn-success text-white mt-2">Rechercher</button>
			{{ form_end(form) }}
		</div>

		{% include 'home/_coach.html.twig' %}
		<div class="d-flex align-items-baseline my-5">
			<i class="bi bi-caret-left-fill text-primary"></i>
			<p class="text-uppercase fw-bold fst-italic">
				<a href="{{ path('home') }}">Retour à la page d'accueil</a>
			</p>
		</div>
	</section>
{% endblock %}
