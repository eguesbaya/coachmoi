{% extends 'layout_superadmin.html.twig' %}

{% block title %} Ajouter espace{% endblock %}

{% block content %}
    <h1> Espace </h1>
    <div class="container">
        <div class="border border-dark m-2">
            <div class="profile-h2 d-flex align-items-center bg-dark rounded w-75 p-0">
                <h2 class="m-2 text-light text-truncate"><i class="bi bi-geo-alt-fill text-success me-2"></i> Ajouter</h2>
            </div>
            {{ include('admin_space/_form.html.twig') }}
            </div>
            <div class="d-flex justify-content-end m-2">
                <a class="btn btn-success text-dark" href="{{ path('admin_space_index') }}"><i class="bi bi-arrow-counterclockwise text-dark"></i> Retour liste des espaces</a>
            </div>
        </div>
    </div>
{% endblock %}
