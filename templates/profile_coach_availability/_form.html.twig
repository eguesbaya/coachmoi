{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-success text-white mt-3"><i class="bi bi-check-square-fill text-white me-2"></i>{{ button_label|default('Enregistrer') }}</button>
{{ form_end(form) }}
